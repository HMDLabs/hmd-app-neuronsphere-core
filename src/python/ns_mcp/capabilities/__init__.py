"""MCP protocol capabilities other than tools: resources and prompts.

Kept out of ``ns_mcp/tools/`` deliberately: ``tools/resources.py`` is about
*NeuronSphere* resources (namespaced dependency types), which are a different
thing entirely from MCP protocol resources.
"""
